<launch>

  <!-- We resume the logic in empty_world.launch, changing only the name of the world to be launched -->
  <include file="$(find recognizer)/launch/recognizer.launch">
    <arg name="voice_commands_topic" value="vel_cmd"/>
  </include>

</launch>
