/**
 *
 *
 *
 *
 */

#ifndef F_H
#define F_H
#include <complex>
#include <valarray>


using namespace std;

class Filter {
public :
    Filter(float *b, int length);
    ~Filter();
    float filtering(float input);
    
private:
    float *b;
    float *d;
    int length;
};

#endif
